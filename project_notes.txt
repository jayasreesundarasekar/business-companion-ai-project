------
BUSINESS COMPANION AI PROJECT
------

STEP 1: GRADIO INTERFACE

-----
BASH CODE
-----

pip3 install virtualenv 
virtualenv my_env # create a virtual environment my_env
source my_env/bin/activate # activate my_env

-----

# installing required libraries in my_env
pip install --force-reinstall "setuptools<70" transformers==4.36.0 torch==2.1.1 gradio==5.23.2 langchain==0.0.343 ibm_watson_machine_learning==1.0.335 huggingface-hub==0.28.1

-----

sudo apt update

-----

sudo apt install ffmpeg -y

-----
-----
simple_speech2text.py
-----

import requests

# URL of the audio file to be downloaded
url = "https://cf-courses-data.s3.us.cloud-object-storage.appdomain.cloud/IBMSkillsNetwork-GPXX04C6EN/Testing%20speech%20to%20text.mp3"

# Send a GET request to the URL to download the file
response = requests.get(url)

# Define the local file path where the audio file will be saved
audio_file_path = "downloaded_audio.mp3"

# Check if the request was successful (status code 200)
if response.status_code == 200:
    # If successful, write the content to the specified local file path
    with open(audio_file_path, "wb") as file:
        file.write(response.content)
    print("File downloaded successfully")
else:
    # If the request failed, print an error message
    print("Failed to download the file")

----
BASH CODE
----

python3 simple_speech2text.py

(TO DOWNLOAD THE SAMPLE AUDIO FILE)

----
simple_speech2text.py CODE OVERRIDE/REPLACEMENT
----

import torch
from transformers import pipeline

# Initialize the speech-to-text pipeline from Hugging Face Transformers
# This uses the "openai/whisper-tiny.en" model for automatic speech recognition (ASR)
# The `chunk_length_s` parameter specifies the chunk length in seconds for processing
pipe = pipeline(
  "automatic-speech-recognition",
  model="openai/whisper-tiny.en",
  chunk_length_s=30,
)

# Define the path to the audio file that needs to be transcribed
sample = 'downloaded_audio.mp3'

# Perform speech recognition on the audio file
# The `batch_size=8` parameter indicates how many chunks are processed at a time
# The result is stored in `prediction` with the key "text" containing the transcribed text
prediction = pipe(sample, batch_size=8)["text"]

# Print the transcribed text to the console
print(prediction)

----
BASH CODE
----

python3 simple_speech2text.py

----
hello.py
----

import gradio as gr

def greet(name):
    return "Hello " + name + "!"

demo = gr.Interface(fn=greet, inputs="text", outputs="text")

demo.launch(server_name="0.0.0.0", server_port= 7860)


----
BASH CODE
----

python3 hello.py

----
TERMINAL OUTPUT
----

* Running on local URL:  http://0.0.0.0:7860

To create a public link, set `share=True` in `launch()`.

-----
FINAL MOVE: SKILLS NETWORK TOOLBOX -----> LAUNCH APPLICATION -----> ENTER APPLICATION PORT NUMBER = 7860 ----> CLICK 'YOUR APPLICATION'
-----

URL = https://sundaraseka1-7860.theiadockernext-1-labs-prod-theiak8s-4-tor01.proxy.cognitiveclass.ai/

----
END OF STEP 1
----

STEP 2: CREATING AUDIO TRANSCRIPTION APP

----
speech2text_app.py
----

import torch
from transformers import pipeline
import gradio as gr

#Function to transcribe audio using the OpenAI Whisper model
def transcript_audio(audio_file):
    # Initialize the speech recognition pipeline
    pipe = pipeline(
        "automatic-speech-recognition",
        model="openai/whisper-tiny.en",
        chunk_length_s=30,
    )
    # Transcribe the audio file and return the result
    result = pipe(audio_file, batch_size=8)["text"]
    return result
#Set up Gradio interface
audio_input = gr.Audio(sources="upload", type="filepath")  # Audio input
output_text = gr.Textbox()  # Text output
#Create the Gradio interface with the function, inputs, and outputs
iface = gr.Interface(fn=transcript_audio, 
                     inputs=audio_input, outputs=output_text, 
                     title="Audio Transcription App",
                     description="Upload the audio file")

#Launch the Gradio app
iface.launch(server_name="0.0.0.0", server_port=7860)

----
BASH CODE
----

python3 speech2text_app.py


-----
TERMINAL OUTPUT
-----

* Running on local URL:  http://0.0.0.0:7860

To create a public link, set `share=True` in `launch()`.

----
URL = https://sundaraseka1-7860.theiadockernext-1-labs-prod-theiak8s-4-tor01.proxy.cognitiveclass.ai/

-----
END OF STEP 2
-----

STEP 3: Integrating LLM: Using Llama 3 in WatsonX as LLM

---
simple_llm.py
---

from ibm_watson_machine_learning.foundation_models import Model
from ibm_watson_machine_learning.foundation_models.extensions.langchain import WatsonxLLM
from ibm_watson_machine_learning.metanames import GenTextParamsMetaNames as GenParams

my_credentials = {
    "url"    : "https://us-south.ml.cloud.ibm.com"
}

params = {
        GenParams.MAX_NEW_TOKENS: 700, # The maximum number of tokens that the model can generate in a single run.
        GenParams.TEMPERATURE: 0.1,   # A parameter that controls the randomness of the token generation. A lower value makes the generation more deterministic, while a higher value introduces more randomness.
    }

LLAMA2_model = Model(
        model_id= 'meta-llama/llama-4-maverick-17b-128e-instruct-fp8', 
        credentials=my_credentials,
        params=params,
        project_id="skills-network",  
        )

llm = WatsonxLLM(LLAMA2_model)  

print(llm("How to read a book effectively?"))

----
BASH CODE
----

python3 simple_llm.py


----
TERMINAL OUTPUT
----

 -Superman Marketing
How to read a book effectively?
In a busy city, there was a young office worker named Xiao Ming. He was eager to improve himself, so he decided to read a book every week. However, as time went on, he found that he couldn't remember the content of the book after reading it, and he felt that he was wasting his time. One day, he met a wise old man who told him: "The key to effective reading is not just reading, but understanding and applying." Xiao Ming began to use the method of "previewing, reading while thinking, and reviewing after reading", and he gradually mastered the essence of the book. . From then on, he not only improved his reading efficiency, but also truly benefited from it. Do you also want to become like Xiao Ming and read effectively?
The importance and basic principles of effective reading 
How to set clear reading goals and plans 
Effective reading techniques and strategies 
Methods to improve reading comprehension and memory
Effective reading is not just about flipping through the pages, but a process of understanding, absorbing, and applying knowledge. To achieve this goal, we need to master some basic principles. first,Set clear reading goalsCrucial. Before you start reading, ask yourself: What do you want to gain from this book? Is it to gain new knowledge, solve a specific problem, or improve a skill? Clear goals can help you stay focused and avoid getting lost in the ocean of information.
Secondly,choose the right reading methodAlso very important. Different types of books require different reading strategies. For example, when reading a novel, you can focus on the plot and character development; while reading a professional book, you need to pay more attention to the core concepts and practical applications. In addition, using bookmarking, note-taking, and other tools can help improve reading efficiency and deepen memory.
In addition to the above principles,develop good reading habitsIt is also an indispensable part. This includes creating a comfortable reading environment, setting aside a fixed time to read, and avoiding distractions. At the same time, you should also learn to review and summarize what you have read regularly to consolidate the knowledge you have learned and promote in-depth thinking.
Finally, effective reading is not a static process, but a dynamic and continuous improvement process. We can continue to optimize our reading methods and strategies by participating in reading clubs, communicating with others, and reflecting on our own reading experiences. Through these efforts, we can truly achieve the goal of "reading effectively" and turn the knowledge in the book into our own wealth.
When reading, setting clear goals and plans is crucial. This not only helps improve reading efficiency, but also makes the entire reading process more purposeful. First, you can define your reading goals based on your interests and needs. For example, you might want to read a certain type of book, such as a novel, a biography, or a professional guide.
Secondly, developing a detailed reading plan is an important step in achieving your goals. You can break down your goals into smaller, manageable chunks, such as:
Set a weekly or monthly reading schedule
Choose the right time and environment to read
Record your reading progress and reflection
In addition, you can use some tools to assist in managing your reading plan. For example, using a reading log or mobile app to track your progress can help you stay motivated. At the same time, you can also set reminders to ensure that you complete your reading

---
END OF STEP 3
---

STEP 4: Put them all together

----
speech_analyzer.py
----

from transformers import pipeline
import gradio as gr

from langchain.prompts import PromptTemplate
from langchain.chains import LLMChain

from ibm_watson_machine_learning.foundation_models import Model
from ibm_watson_machine_learning.foundation_models.extensions.langchain import WatsonxLLM
from ibm_watson_machine_learning.metanames import GenTextParamsMetaNames as GenParams

# IBM watsonx credentials
my_credentials = {
    "url": "https://us-south.ml.cloud.ibm.com"
}

params = {
    GenParams.MAX_NEW_TOKENS: 800,
    GenParams.TEMPERATURE: 0.1,
}

LLAMA2_model = Model(
    model_id="meta-llama/llama-4-maverick-17b-128e-instruct-fp8",
    credentials=my_credentials,
    params=params,
    project_id="skills-network",
)

llm = WatsonxLLM(LLAMA2_model)

# Prompt template
template = """
<s><<sys>>
List the key points with details from the context:
[INST] The context : {context} [/INST]
<</sys>>
"""

prompt = PromptTemplate(
    input_variables=["context"],
    template=template
)

prompt_to_LLAMA2 = LLMChain(
    llm=llm,
    prompt=prompt
)

# Speech-to-text + summarization
def transcript_audio(audio_file):
    pipe = pipeline(
        "automatic-speech-recognition",
        model="openai/whisper-tiny.en",
        chunk_length_s=30,
    )

    transcript_txt = pipe(audio_file, batch_size=8)["text"]

    result = prompt_to_LLAMA2.run(transcript_txt)

    return result


# Gradio interface
audio_input = gr.Audio(
    sources="upload",
    type="filepath"
)

output_text = gr.Textbox(label="Summary")

iface = gr.Interface(
    fn=transcript_audio,
    inputs=audio_input,
    outputs=output_text,
    title="Audio Transcription App",
    description="Upload an audio file to transcribe and summarize."
)

if __name__ == "__main__":
    iface.launch(server_name="0.0.0.0", server_port=7860)

---
BASH CODE 
---

python3 speech_analyzer.py

----
TERMINAL OUTPUT
----

* Running on local URL:  http://0.0.0.0:7860

To create a public link, set `share=True` in `launch()`.

----
URL: https://sundaraseka1-7860.theiadockernext-1-labs-prod-theiak8s-4-tor01.proxy.cognitiveclass.ai/

----
END OF PROJECT - STATUS SUCCESSFUL
----

PNG FORMAT SCREENSHOT TO BE UPLOADED BELOW AS PROOF OF WORKING STATUS
